import { DescriptionCutPipe } from './description-cut.pipe';

describe('DescriptionCutPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionCutPipe();
    expect(pipe).toBeTruthy();
  });
});
